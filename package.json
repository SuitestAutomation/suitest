{
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "lint": "lerna run lint",
    "coverage": "lerna run coverage",
    "postinstall": "lerna bootstrap --hoist"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^3.16.4"
  },
  "devDependencies": {
    "husky": "3.0.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint",
      "pre-push": "npm test"
    }
  }
}
